{
  "name": "chess-mottys-version",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "Chess (Motty's Version). Real chess, fair engine, except two random pieces teleport before every turn.",
  "scripts": {
    "dev": "node scripts/serve.mjs",
    "test": "node test/run-all.mjs"
  }
}
