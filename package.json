{
  "name": "chess-mottys-version",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "Chess (Motty's Version). Real chess against a fair bot, with one random non-king teleport after every move.",
  "scripts": {
    "dev": "node scripts/serve.mjs",
    "test": "node test/run-all.mjs"
  }
}
